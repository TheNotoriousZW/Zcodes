import os
import pathlib
import shutil

# commands

# git config --global user.name "Zaphon Wray"
# git config --global use.email "zaphwray@gmail.com"
# git config --global init.default branch main
# git config -h (help)
# git status (tells status of the repository)
# git add 'filename' (to track a file)
# git rm --cached 'filename' (to untrack file)
# git add --a (track all the files)
# git diff (shows the difference from the last commit)
# git restore --staged 'filename' (restores and moves file back to working file)
# git log (shows all the previous commits)
# git branch 'name of new branch' (creates a sub branch that you can work on before rejoining the main branch)
# git switch 'branch name' (switches to the working branch)
# git config --list (shows the list of configurations made)
# git reset "file name" (reset file back to working stage)
# git reset (reset all files back to working stage)
# git clone (url of the clone), (where you want it cloned)
# git remote -v (list the information of the path)
# git branch -a (list all branches locally and remotely)
# git checkout -b "branch name" (make a new branch)
# git merge master (takes all the changes from the master branch and merges it with the working branch)
# git pull
# git push
# git commit -m ("message)



